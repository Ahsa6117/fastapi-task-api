# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

POSTGRES_USER=taskapi
POSTGRES_PASSWORD=changeme
POSTGRES_DB=taskapi

# Used by the api service to connect to the db service.
# Host "db" matches the service name in compose.yaml.
DATABASE_URL=postgresql://taskapi:changeme@db:5432/taskapi
