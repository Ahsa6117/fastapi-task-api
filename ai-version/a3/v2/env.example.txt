POSTGRES_USER=taskapi
POSTGRES_PASSWORD=changeme
POSTGRES_DB=tasks

# Used by the api service inside docker compose; host "db" is the service
# name on the compose network, not localhost.
DATABASE_URL=postgresql://taskapi:changeme@db:5432/tasks
