"""Assignment A4 -- a secured API built on Supabase Auth.

Kept in its own package so the A1-A3 Task API in the repo root keeps
working untouched. Run it with:

    uvicorn auth.main:app --reload --port 8000
"""
